# INBOX - orders for the next session (tick when done)

- [ ] Add pluggable pitch-detection backends behind one interface: a CREPE option + a pYIN fallback (pYIN can be the lightweight default), selectable by config. Read the training pipeline + PROJECT.md, implement the abstraction + both backends + a tiny test, tick the .mc subphase, commit+push.  *(queued 2026-06-25 via Pantheon - the Aleph)*
